# Working on Nightsky

Start with [REMIXING.md](REMIXING.md) and the single **Make your own copy** agent prompt in the app. Read the carried `nightsky-remix-guide` and inspect the destination's native schemas before changing a song, visuals, audio analysis or shared listening. Add verified audio/artwork with `tonk blob add`, query `nightsky-track`, and use the native `nightsky-song-add`, `nightsky-song-select` or `nightsky-song-update` command. Metadata edits preserve track identity; a new recording gets a new track entity and previous comments remain attached to their original track.

Use `npx --yes @tonk/cli@0.6.14` and its native help. If the exact reference scope cannot be read, ask for its Share invite first; scope/workspace identifiers alone grant no access. Do not search unrelated old projects or replicas as a substitute. A fresh working folder is normally enough. Reuse the user's current identity/account unless they requested isolation; `TONK_SPACES_STATE` changes registry/replicas, not device identity. Native facts, code and media may fetch on demand after pull.

Resolve the `id:nightsky-remix-guide-main` name to one entity, then read only that entity's `nightsky-remix-guide.markdown`. Do not choose among historical guide rows by order. The current guide contains the precise native source recipe: join current names to component modules, preserve `command!:` transience, retain authored rule joins/effects, then compile every view facet after commands exist. The tool manifest is the `nightsky-agent-file` row on the supplied workspace with path `agent-files.manifest.json`; select its exact bundle/version.

Bundle any missing song, visual and shared-listening preferences once and honor existing answers. For a personal remix, create a new owned space, preserve the reference and use a fresh UUID `id:` workspace. Copy only chosen native media with verified byte readback, leave source membership/relay credentials and unrequested comments behind, and preserve previous recordings/comments in the destination. Verify subject, owner, remote and successful push before calling a space hosted; signed-in status alone is insufficient. Login can reuse authorization without opening a window. Set Nightsky as the home only in the newly created blank space; preserve meaningful existing homes. Verify the invite's landing route, then return the user-requested Share invite in chat. Deliberately shared Tonk invitations are distinct from relay/API/account secrets.

Do not use a blind CSV space export/import for this: it includes authority and application data, and CLI 0.6.14 cannot round-trip long compiled-rule byte values. Prefer inspected authored notation and check the resulting commands, component source and views. No custom copy program is required. After copying, verify one mounted player, local playback, seeking, adding a new song and the independent comment state, then give the user the new space's share invitation.

The native agent-file bundle carries these instructions, exact audio-module inputs, guarded component tools and optional relay setup files. For deeper work that needs local tools, optionally materialize the verified current bundle into a fresh local folder; no separate website download is required. It does not execute automatically. In the catalog checkout, `core.yaml` and optional `demo*.yaml` are the original install notation. Repeated YAML heads are intentional: do not round-trip them through a normal YAML mapping parser.

For a component edit, export the exact named component from the intended space with `tools/component.mjs`, edit its `.js`, then generate a guarded change plan. Keep the snapshot unchanged. Dry-run the plan, require one matching original component, evaluate, and verify the resulting source. Do not reinstall all of `core.yaml` for one visual change or broadly retract components. Preserve unrelated applications, facts and names.

Use `globalThis.Nightsky`, `nightsky-*` elements/events and room-scoped listeners. Components must tolerate any loading order and clean up on disconnect. Keep audio scheduling in `nightsky-player` and live transport in `nightsky-live`. Tonk holds durable tracks, text/GIF dialogue and relay discovery; WebRTC carries playback and presence. Do not add playback facts, a countdown or cursor history.

Edit `tools/examples/*.mjs` to change audio algorithms; rebuild only the generated block of an exported `nightsky-kit` with the supplied namespaced builder. Never edit that generated block or its version by hand. Changed audio bytes or analysis algorithms require newly generated, identity-matched sidecars. Missing features must leave playback and basic visuals working.

Preserve immediate rhythmic motion separately from gentle brightness, deterministic seek/pause, the fixed orbital pole, the blue-white/ivory/sparse-amber palette, reduced motion, and Canvas2D/DOM fallbacks unless the user asks to change them. Choose one timeline appearance while retaining click-to-seek and drag-to-comment contracts.

After changes, check the affected interaction in a fresh browser. For transport changes use two real clients; synthetic timing is not physical loudspeaker alignment. Keep relay/account credentials and private media out of source, screenshots and public examples. Installing this template must not overwrite another app's carried `AGENTS.md` or a meaningful existing home. A newly created blank copy should open Nightsky directly.
